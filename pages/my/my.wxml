<!--pages/my/my.wxml-->
<image src="{{imageUrl}}" bind:tap="handleImage"></image>
<map class="map" longitude="{{longitude}}" latitude="{{latitude}}" 
scale="{{scale}}" show-compass="{{showCompass}}" markers="{{markers}}"
polyline="{{polyline}}" circles="{{circles}}"></map>